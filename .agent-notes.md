
## حالة العمل 2026-07-17 (تسجيل الدخول/التسجيل بحساب ERPNext)
- server/erpAuth.ts: verifyErpCredentials + loginWithErpAccount + signupWithErpAccount (تجربة 3 أيام، حفظ اتصال مشفر، خطة planId)
- server/routers.ts auth: loginWithErp (email,password,erpUrl?) وsignupWithErp (erpUrl,email,password,planId,companyName?,phone?) — كلاهما يصدر كوكي COOKIE_NAME عبر sdk.createSessionToken(openId,{name})
- client: pages/Login.tsx (يوجه /dashboard)، pages/Signup.tsx (خطوتان: بيانات النظام ثم الباقات من trpc.plans.list)، App.tsx: "/" => Home، /login، /signup
- الباقات: 1 الأساسية 299 SAR، 2 الاحترافية 699، 3 المؤسسية 1499 (شهرياً)
- متبقٍ: التحقق من tsc، اختبارات vitest (erpAuth مع mock fetch)، لقطات شاشة، checkpoint، تحديث todo.md
- رابط النشر: https://erpnextai-wypcefat.manus.space

## حالة 2026-07-17 (إصلاح واجهة الجوال)
- Dashboard.tsx: SidebarContent + Sidebar متجاوب (شريط علوي جوال h-14 مع Sheet درج جانبي side=right فيه تسجيل الخروج)
- طُبق flex-col md:flex-row وp-4 md:p-8 على Dashboard/Invoices/Tasks/Subscription/AdminPanel/AccountSettings
- المستخدم أبقى الحسابات الثلاثة (لم يُحذف شيء)؛ يريد فقط تسجيل خروج وتسجيل جديد
- المتبقي: التحقق من درج الجوال المفتوح (زر تسجيل الخروج داخل Sheet)، ثم checkpoint وتسليم
- آخر checkpoint: 4ef836e9 | نشر: https://erpnextai-wypcefat.manus.space | اختبارات: 52 ناجحة

## حالة 2026-07-17 (ميزة Quick Replies)
- الخلفية مكتملة: agent.ts فيه extractQuickReplies() مُصدَّرة (سطر ~35 بعد currentErpConfig)، system prompt فيه قسم [QUICK_REPLIES: ...]، chat يرجع quickReplies ويحفظها بصيغة JSON {toolResults, quickReplies} في عمود toolResults
- ملاحظة: openConversation في AgentChat.tsx (سطر ~461) يعمل JSON.parse على m.toolResults كمصفوفة ToolResult[] — يجب التعامل مع الصيغتين القديمة (مصفوفة) والجديدة (كائن {toolResults, quickReplies})
- الواجهة المتبقية: AgentChat.tsx — Message type (سطر 20) أضف quickReplies?: string[]; send() سطر 503-532 استقبل result.quickReplies; عرض الأزرار بعد آخر رسالة assistant فقط (i === messages.length - 1 && !chatMutation.isPending) وإرسال الخيار عبر send(option)
- بعدها: اختبارات vitest لـ extractQuickReplies + checkpoint
- todo.md: قسم "أزرار الإجابات السريعة" فيه 5 بنود غير مكتملة

## نظام الإشعارات (2026-07-18)
- VAPID keys محفوظة كأسرار: VITE_VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY (مولدة بنجاح)
- web-push + @types/web-push مثبتة
- جدولا notifications + push_subscriptions منشآن في DB (migration 0007 مطبق)
- server/notifications.ts: notifyUser/notifyAdmins/maybeNotifyTrialEnding/listNotifications/unreadCount/markRead/markAllRead/deleteNotification/savePushSubscription/removePushSubscription/sendPushToUser
- getDb() قد يعيد null → استخدم requireDb() المحلية
- المتبقي: راوتر tRPC notifications، ربط الأحداث (فاتورة/مهمة/تجربة/مستخدم جديد)، service worker push handler (المشروع فيه VitePWA)، جرس UI، زر تفعيل push، اختبارات
- الأحداث: agent invoice في server/routers/agent.ts (create_invoice/submit_invoice tools)، مهمة مكتملة في tasks router (routers.ts)، تسجيل جديد في erpAuth.ts signupWithErpAccount، trial في subscription.get (routers.ts)
- vite.config.ts فيه VitePWA مع workbox maximumFileSizeToCacheInBytes 5MiB — سأحتاج injectManifest أو استخدام ملف sw إضافي؟ تحقق من إعداد VitePWA الحالي
- notificationsRouter مسجل في appRouter تحت notifications
- الأحداث المربوطة: new_user (signup في routers.ts)، task_completed (tasks.updateStatus)، trial_ending (subscription.get)
- المتبقي: إشعار فاتورة في agent.ts (case "create_invoice" سطر ~718 وcase "submit_invoice" ~866 داخل executeTool — تحقق هل يمر userId هناك)، ثم UI: جرس NotificationBell + تفعيل push في client، ثم اختبارات
- push-sw.js في client/public + importScripts في vite.config.ts workbox
- NotificationBell.tsx منشأ في client/src/components (جرس + عدّاد + قائمة + زر تفعيل push عبر pushManager.subscribe مع VITE_VAPID_PUBLIC_KEY)
- إشعارات الفواتير مربوطة في agent.ts (بعد executeTool: __INVOICE_CREATED__/__INVOICE_SUBMITTED__)
- notifications.ts في server مكتمل: notifyUser/notifyAdmins/maybeNotifyTrialEnding/list/unread/markRead/markAllRead/delete/savePushSubscription/removePushSubscription
- خطأ vite قديم عن InstallAppButton هو stale (الملف موجود export function InstallAppButton)
- Dashboard.tsx: SidebarContent يحوي InstallAppButton؛ الهيدر المحمول في مكوّن Sidebar (زر ☰) — سأضع NotificationBell في: 1) الهيدر المحمول للـ Sidebar 2) هيدر AgentChat بجانب زر الصوت (سطر ~738) 3) أعلى صفحات Dashboard للشاشات الكبيرة؟ الأبسط: في هيدر الموبايل + AgentChat + أعلى محتوى Dashboard
- المتبقي: دمج الجرس في الواجهة، اختبارات notifications، pnpm test، checkpoint
- الجرس مدمج ويظهر بصرياً: dashboard (أعلى يسار سطح المكتب + هيدر الجوال) وAgentChat (بجانب شارة متصل). لقطات الشاشة سليمة.
- notificationsRouter مسجل: list/unreadCount/markRead/markAllRead/delete/subscribePush/unsubscribePush — كلها protectedProcedure
- المتبقي للمرحلة 4: كتابة server/notifications.test.ts، pnpm test، وسم todo، checkpoint، رسالة نهائية

## GitHub upload issue (Jul 18)
- User wants project uploaded to GitHub. Management UI export button disabled.
- GitHub connector uid: bbb0df76-66bd-4a24-ae4f-2aac4750d90b, enabled=true, tokenReplacementEnabled=true (restored).
- Old linked account: dreslamat-ai. Token was ghu_ (GitHub App user token) with NO repo-creation permission (403 Resource not accessible by integration), then became 401 Bad credentials after user revoked authorization from GitHub account side.
- User revoked GitHub app authorization to re-authenticate; wants to finish from here (chat), not management UI.
- NEXT: user must re-connect GitHub via Manus Settings -> Connected apps, granting All repositories access. Then: gh api user to verify; gh repo create almoaser-ai-agent --private; git remote add github <url>; git push (project git repo at /home/ubuntu/erpnext-ai-agents, branch main, remote origin = manus internal; do NOT touch origin).
- Latest checkpoint/version: 3da33595. Site: https://almoaser.manus.space

## GitHub upload RESOLVED (Jul 18)
- User provided classic PAT in chat (told to revoke it after). Uploaded via separate remote `github`.
- Repo: https://github.com/dreslamat-ai/almoaser-ai-agent (private), branch main, latest commit 3da33595.
- Token removed from remote URL after push. Manus GitHub connector token still broken (401) — irrelevant now.

## Plans + Credits + MyFatoorah work (Jul 18, in progress)
- DONE DB: plans has maxDocuments, monthlyCredits, yearlyDiscountPct(15), hasDirectSupport. subscriptions has billing(monthly|yearly), creditsBalance, creditsCycleStart. New tables: credit_transactions(type: message|document|monthly_refill|topup|adjustment, amount, balanceAfter), payments(purpose: subscription|topup, planId, billing, credits, amount, status pending|paid|failed|expired, invoiceId, paymentUrl).
- DONE data: plan1=999SAR/30docs/150cr, plan2=1499/50/250, plan3=2499/100/500+hasDirectSupport. Features JSON updated in Arabic. Existing subs refilled creditsBalance=plan credits, cycleStart=now.
- DONE server/credits.ts: MESSAGE_COST=1, DOCUMENT_COST=5, topup 100cr=100SAR (isValidTopupCredits multiples of 100), yearlyPrice(), ensureCreditsCycle (30-day refill, resets to monthlyCredits, no carryover), deductCredits (throws InsufficientCreditsError w/ Arabic msg), addTopupCredits, getCreditTransactions.
- DONE agent.ts chat: deducts 1 credit per message at start; FORBIDDEN error if insufficient; skips silently on db errors.
- TODO next: deduct 5 credits per document created (in executeTool cases create_invoice/create_customer? NO — user said per document; apply to create_invoice, create_purchase_invoice, create_payment_entry, create_journal_entry — decide: docs = ERP documents created), server/myfatoorah.ts (SendPayment v2/ExecutePayment + GetPaymentStatus, base https://api.myfatoorah.com or apitest.myfatoorah.com, Bearer MYFATOORAH_API_KEY), payments router (createSubscriptionPayment w/ billing monthly|yearly w/15% discount via yearlyPrice, createTopupPayment, callback GET /api/payment/callback?paymentId= → GetPaymentStatus → mark paid → activate sub or addTopupCredits), request MYFATOORAH_API_KEY + MYFATOORAH_BASE_URL via webdev_request_secrets (user will provide when ready; asked to prepare).
- UI TODO: Home PricingSection (monthly/yearly toggle w/15% discount, new features), Signup step2 (billing choice + credits display), Subscription page (balance card, topup buttons 100/200/500 credits, billing toggle, payment redirect), AgentChat maybe show balance. Dashboard balance widget.
- Tests TODO: credits logic tests + payment activation logic tests. 67 existing tests must stay green.
- GitHub repo: dreslamat-ai/almoaser-ai-agent (private) synced at cda664e7. User PAT was provided in chat (revoke advised) - do NOT reuse; ask again if needed.

## Progress Jul 18 (continued)
- DONE: server/myfatoorah.ts (SendPayment v2 + GetPaymentStatus, env MYFATOORAH_API_KEY + MYFATOORAH_BASE_URL default apitest). server/routers/payments.ts: isConfigured, createSubscriptionPayment (billing monthly|yearly, 15% off via yearlyPrice), createTopupPayment (multiples of 100cr, 1 SAR/cr), verifyPayment (PaymentId → activate sub w/endDate or addTopupCredits), list. Registered in appRouter as payments.
- DONE: credits router (balance, transactions) in routers.ts. subscription.create/upgrade accept billing + refill creditsBalance.
- DONE: agent chat deducts 1cr/message + 5cr per created doc (create_invoice/purchase_invoice/payment_entry/journal_entry, skips error/needs_clarification/duplicate_prevented results).
- DONE UI: Home HowItWorksSection rewritten to 4 self-service steps (register → pick plan+3d trial → auto-connect ERP → chat). Home PricingSection: monthly/yearly toggle w/15% badge, yearly per-month + total display, docs+credits chips.
- DONE UI: Subscription.tsx rebuilt: credits balance card w/progress bar + topup buttons (100/200/500) → payments.createTopupPayment redirect (toast "قيد التفعيل" if not configured), billing toggle, history of credit txns, upgrade/create pass billing.
- SECRETS: MYFATOORAH request was INTERRUPTED - user said to DEFER until they get keys from MyFatoorah. Do NOT request again until user provides.
- REMAINING: /payment/callback page (client route reading paymentId query → payments.verifyPayment), Signup step2 credits/billing display (optional), Dashboard balance widget (optional), vitest tests for credits+payments, tsc check, screenshots, checkpoint.

## Jul 18 session 2 — VAT + settings + invoice design
USER INVOICE TEMPLATE (uploaded /home/ubuntu/upload/IMG_1861.HEIC — Horizon sample):
- Header: logo top-left, company name Arabic top-right, curved navy shape top-right corner + bottom-left corner
- Title center: "فاتورة ضريبية" + "VAT: <15-digit seller VAT>" + invoice no (ACC-SINV-2026-00048)
- Info row (RTL): اسم العميل | رقم التسجيل الضريبي (customer VAT) | عنوان العميل | تاريخ الفاتورة
- Items table (RTL cols): س | الخدمة | الفترة(UOM Nos) | العدد | السعر | الإجمالي — watermark logo behind
- Summary table (right labels): الإجمالي غير شامل الضريبة / الخصم / الضريبة / المجموع الإجمالي / ملاحظات + QR code on right side
- Footer: Phone + Email + Address (Riyadh) with icons, navy accents
STATUS DONE:
- create_invoice now auto-applies default Sales Taxes and Charges Template from ERP (taxes_and_charges + taxes rows fetched from template; apply_vat param default true)
- create_customer accepts tax_id (15-digit Saudi VAT, starts/ends with 3), passed to ERP; tool descriptions updated
- VAT disclaimer added: Home pricing, Subscription page, Signup step2; Signup shows docs+credits chips
- /payment/callback route + PaymentCallback.tsx done; payments router (MyFatoorah deferred, isConfigured gate)
- Home: HowItWorks 4 self-service steps; PricingSection monthly/yearly toggle
REMAINING:
1. system prompt update in agent.ts: tax comes ready from ERP templates, agent has settings-management powers; mention asking tax_id for company customers
2. NEW agent tools: get_settings/update_settings (generic doctype settings read/update: Company, Selling Settings, Buying Settings, Stock Settings, Accounts Settings, System Settings, tax templates list) — user wants agent full config powers for all modules
3. Invoice print design: ErpInvoices page / Invoices page — professional ZATCA-style print view matching template above (with QR TLV base64 ZATCA format if possible, seller name/VAT/timestamp/total/VAT amount)
4. Subscription page: billing switch button showing yearly amount + saved discount value (partially done w/ toggle; user wants explicit convert button showing amounts)
5. Tests (credits, payments), tsc, screenshots, checkpoint (67 tests must stay green)
- MyFatoorah keys: DEFERRED until user provides. Do NOT re-request.

## Jul 18 session 2 progress (part 2)
DONE since last note:
- get_settings/update_settings agent tools added (tool defs after delete_document + executeTool cases; SINGLE_DOCTYPES set for Selling/Buying/Stock/Accounts/System Settings + Global Defaults; Company & tax templates by name or auto-single). tsc clean.
- System prompt: rules 14 (VAT auto from ERP template, apply_vat=false for exempt, report net/tax/grand) + 15 (ask tax_id 15-digit for company customers); settings powers paragraph added.
- server/routers.ts: erpnext.getInvoicePrintData procedure (invoice + company + customer + ZATCA TLV qrBase64 via Buffer tags 1-5).
- pnpm add qrcode.react (4.2.0).
- client/src/components/InvoicePrintView.tsx created: modal + print window (IBM Plex Sans Arabic), navy curved corners (#1a2744/#2d4a7a), header logo /manus-storage/logo_icon_ba8a6fa6.png + company name, "فاتورة ضريبية" + VAT + inv no, 4-col customer info row, items table (س/الخدمة/الوحدة/العدد/السعر/الإجمالي) w/ watermark, summary table + QRCodeSVG(qrBase64), footer phone/email/country.
REMAINING:
1. Wire InvoicePrintView into ErpInvoices.tsx (add عرض/طباعة button per row) and maybe Invoices.tsx
2. Subscription.tsx: explicit "التحويل إلى سنوي" button on current active sub showing yearly amount + discount saved (user asked explicitly); current page has billing toggle for new plans but needs convert-existing button w/ amounts
3. tests (credits/payments), pnpm test (67 must stay green), tsc, screenshots (/, /subscription, /erp/invoices), checkpoint (auto-publish)
- MyFatoorah DEFERRED. GitHub repo dreslamat-ai/almoaser-ai-agent exists (push after checkpoint optional w/ user PAT ghp_Ozte... in earlier notes - actually token was in chat; repo push done earlier).
