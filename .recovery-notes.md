# ملاحظات إصلاح تلف نظام الملفات — Jul 21-22, 2026

## الحالة
- القرص /dev/vda (ext4) تلف جزئياً: 106+ أخطاء EXT4 في dmesg (bad directory entries, doubly allocated inodes)
- التلف أصاب: مخزن pnpm (v10)، مجلدات فرعية في .git/objects القديم، و28 ملف مشروع (null bytes)

## ما تم إنجازه
1. ترقية الوكيل بالمهارات الأربع مكتملة: server/routers/agentPersona.ts + دمج في agent.ts (buildExpertSkillsSection) + server/agentPersona.test.ts — TypeScript سليم
2. نسخة احتياطية كاملة للمشروع (بدون node_modules/.git): https://files.manuscdn.com/user_upload_by_module/session_file/310419663029660376/NrRTwBfYgztHQRYK.gz
3. أعيد بناء .git من الصفر (القديم في /tmp/git-corrupted، config في /tmp/git-config-backup) — نقطة تفتيش 173f9328 حُفظت والتاريخ الكامل استُعيد من origin بعد المزامنة
4. استعيدت الملفات الـ28 التالفة من commit f3d7973 (القائمة في /tmp/corrupted-files.txt) — منها patches/wouter@3.7.1.patch وأيقونات PWA وdrizzle snapshots
5. فحص عميق: لا null bytes متبقية في ملفات الكود المتتبعة

## المشاكل المتبقية
- النشر فشل مرتين: ERR_PNPM_LOCKFILE_CONFIG_MISMATCH — لأن نقطة التفتيش 173f9328 حُفظت وملف الباتش كان تالفاً (hash مختلف). الحل: حفظ نقطة تفتيش جديدة بعد استعادة الباتش السليم (hash في lockfile: 4e16e6ff3fde7d6c1024d3e0c8605dc9eb6afb690d0d49958c2f449091813072)
- pnpm install محلياً يفشل: node_modules/.pnpm تالف جزئياً (Failed to remove مجلدات vite/typescript؛ ENOENT عند mkdir). الحل المقترح: نقل node_modules جانباً (mv إلى node_modules-corrupted) وإعادة التثبيت من الصفر بمخزن جديد npm_config_store_dir=/home/ubuntu/.pnpm-store-fresh
- تحذير pnpm 10.4.1 المحلي لا يقرأ pnpm.patchedDependencies من package.json (يتجاهلها) لكن بيئة النشر تستخدمها — لا تغيّر package.json

## خطوات التحقق بعد الإصلاح
1. pnpm install ينجح ويثبت wouter مع الباتش
2. npx tsc --noEmit نظيف
3. pnpm test — 91+ اختباراً (بما فيها agentPersona.test.ts الجديد)
4. webdev_save_checkpoint جديد → النشر ينجح

---

# مهمة جارية: تحويل الوكيل إلى مفتاح OpenAI الخاص بالمستخدم (Jul 22)

## السياق
- الإنتاج يفشل بـ: 412 "your account has hit a usage exhausted" من forge (رصيد Manus منتهٍ)
- المستخدم قدم مفتاح OpenAI (محفوظ كـ OPENAI_API_KEY secret عبر webdev_request_secrets، preventMatching=true)
- المفتاح صالح لكن حساب OpenAI بلا رصيد: 429 insufficient_quota (تحققت 3 مرات عبر check-openai.mjs)
- قرار المستخدم (بعد 3 رسائل "متابعة"): الخيار 3 — نفّذ التحويل الآن، يعمل تلقائياً عند شحن الرصيد، مع fallback للنموذج المدمج

## بنية الكود الحالية
- server/_core/llm.ts: invokeLLM() يرسل إلى `${ENV.forgeApiUrl}/v1/chat/completions` بمفتاح ENV.forgeApiKey (BUILT_IN_FORGE_API_KEY)، مع fetchWithBackoff (4 محاولات)
- server/routers/agent.ts سطر ~1507: `response = await invokeLLM({...})` داخل agent.chat — نقطة الاستدعاء الوحيدة للمحادثة
- ENV في server/_core/env.ts
- ملف check-openai.mjs (مؤقت — احذفه قبل checkpoint النهائي)

## الخطة المتفق عليها
1. عدم تعديل server/_core/llm.ts (framework) — إنشاء server/llmProvider.ts جديد:
   - invokeAgentLLM(): يحاول OpenAI مباشرة (api.openai.com، model gpt-4.1، نفس صيغة chat/completions مع tools) إن وُجد OPENAI_API_KEY
   - عند فشل OpenAI (401/429/5xx): fallback إلى invokeLLM المدمج
   - مفتاح OpenAI يبدأ بـ sk-proj → متغير process.env.OPENAI_API_KEY متاح في dev و production (secret محفوظ)
   - انتبه: ENV.forgeApiKey قد يكون مربوطاً بنفس اسم OPENAI_API_KEY في env.ts — تحقق أولاً!
2. استبدال استدعاء invokeLLM في agent.chat بـ invokeAgentLLM
3. اختبار vitest: server/llmProvider.test.ts (mock fetch: نجاح OpenAI، فشل OpenAI → fallback)
4. حذف check-openai.mjs، تحديث todo.md، checkpoint (auto-publish)

## todo بنود مفتوحة ذات صلة
- بلاغ "تعذر الاتصال بالنموذج" (تشخيص ✓ مكتمل فعلياً — السبب رصيد Manus)
- بنود تحويل OpenAI الأربعة

---

# حالة جارية (Jul 22 لاحقاً): المستخدم قال "تم الشحن" — التحقق من مفتاح OpenAI

## عطل جديد: كل الطرفيات الجديدة تفشل بـ
`failed to create terminal: exec: environment variable contains NUL`
- بدأ بعد إعادة حفظ OPENAI_API_KEY وwebdev_restart_server
- webdev_debug (ثقة عالية): قيمة سر في مخزن env تحتوي NUL byte (بسبب تلف القرص EXT4 السابق)
- أعدت حفظ OPENAI_API_KEY عبر webdev_request_secrets → لم يُصلح الطرفيات
- restart_server ينجح والخادم يعمل؛ لقطات الشاشة تفشل أيضاً

## خطة بديلة (بدون طرفية): إجراء tRPC تشخيصي
- أضفت pingOpenAI() في server/llmProvider.ts (يفحص المفتاح بmax_tokens=1 ويكشف NUL في القيمة، لا يكشف المفتاح)
- التالي: إضافة `diagnostics: router({ openaiPing: publicProcedure... })` في server/routers.ts (appRouter يبدأ سطر 59، imports أعلى الملف)
- ثم استدعاؤه عبر curl على المعاينة أو الإنتاج: POST /api/trpc — أو GET query: /api/trpc/diagnostics.openaiPing
- بعد نجاح التحقق: حذف check-openai.mjs، checkpoint (auto-publish)، اختبار المحادثة في الإنتاج
- تذكير: بيانات ERPNext (demo.almoaser.cloud) لا تزال 401 — بند مفتوح بانتظار المستخدم
---

# حالة (Jul 22): تحديث بيانات ERPNext — النتائج
- المستخدم حفظ ERPNEXT_URL/USERNAME/PASSWORD الجديدة عبر بطاقة الأسرار
- erpPing على dev server: {hasConfig:true, url:"https://demo.almoaser.cloud", status:200, ok:true, loggedInAs:"AI Accountant"} ✓
- openaiPing سابقاً: 200 ok=true ✓ (مفتاح المستخدم gpt-4.1 يعمل بعد الشحن)
- الطرفيات لا تزال معطلة (env NUL) — كل التحقق عبر إجراءات diagnostics بHTTP
- المتبقي: checkpoint (نشر تلقائي) ثم فحص erpPing وopenaiPing على almoaser.manus.space ثم التسليم
---

# مهمة جارية (Jul 22): إخفاء أخطاء المزود عن العملاء
- بلاغ: رسالة "رصيد النموذج الذكي غير كافٍ — اشحن OpenAI" ظهرت لعميل — يجب ألا تظهر
- الموضع: server/routers/agent.ts سطر ~1514-1524 (catch حول invokeAgentLLM)
- notifyAdmins({type,title,body,link}) موجودة في server/notifications.ts (سطر 146) — تُشعر كل الأدمن
- الخطة: رسالة عامة موحدة للعميل + notifyAdmins بالسبب التقني (مع throttle ساعة لمنع إغراق الإشعارات)
- بعدها: فحص openaiPing (هل الرصيد فعلاً نفد رغم نجاح الفحص سابقاً؟) ثم checkpoint
- الطرفيات لا تزال معطلة (env NUL) — التحقق عبر HTTP diagnostics فقط
- فحص الإنتاج batch: /api/trpc/diagnostics.openaiPing,diagnostics.erpPing?batch=1&input=%7B%220%22%3A%7B%22json%22%3Anull%7D%2C%221%22%3A%7B%22json%22%3Anull%7D%7D
