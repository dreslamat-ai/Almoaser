# ملاحظات إصلاح تلف نظام الملفات — Jul 21-22, 2026

## الحالة
- القرص /dev/vda (ext4) تلف جزئياً: 106+ أخطاء EXT4 في dmesg (bad directory entries, doubly allocated inodes)
- التلف أصاب: مخزن pnpm (v10)، مجلدات فرعية في .git/objects القديم، و28 ملف مشروع (null bytes)

## ما تم إنجازه
1. ترقية الوكيل بالمهارات الأربع مكتملة: server/routers/agentPersona.ts + دمج في agent.ts (buildExpertSkillsSection) + server/agentPersona.test.ts — TypeScript سليم
2. نسخة احتياطية كاملة للمشروع (بدون node_modules/.git): https://files.manuscdn.com/user_upload_by_module/session_file/310419663029660376/NrRTwBfYgztHQRYK.gz
3. أعيد بناء .git من الصفر (القديم في /tmp/git-corrupted، config في /tmp/git-config-backup) — نقطة تفتيش 173f9328 حُفظت والتاريخ الكامل استُعيد من origin بعد المزامنة
4. استعيدت الملفات الـ28 التالفة من commit f3d7973 (القائمة في /tmp/corrupted-files.txt) — منها patches/wouter@3.7.1.patch وأيقونات PWA وdrizzle snapshots
5. فحص عميق: لا null bytes متبقية في ملفات الكود المتتبعة

## المشاكل المتبقية
- النشر فشل مرتين: ERR_PNPM_LOCKFILE_CONFIG_MISMATCH — لأن نقطة التفتيش 173f9328 حُفظت وملف الباتش كان تالفاً (hash مختلف). الحل: حفظ نقطة تفتيش جديدة بعد استعادة الباتش السليم (hash في lockfile: 4e16e6ff3fde7d6c1024d3e0c8605dc9eb6afb690d0d49958c2f449091813072)
- pnpm install محلياً يفشل: node_modules/.pnpm تالف جزئياً (Failed to remove مجلدات vite/typescript؛ ENOENT عند mkdir). الحل المقترح: نقل node_modules جانباً (mv إلى node_modules-corrupted) وإعادة التثبيت من الصفر بمخزن جديد npm_config_store_dir=/home/ubuntu/.pnpm-store-fresh
- تحذير pnpm 10.4.1 المحلي لا يقرأ pnpm.patchedDependencies من package.json (يتجاهلها) لكن بيئة النشر تستخدمها — لا تغيّر package.json

## خطوات التحقق بعد الإصلاح
1. pnpm install ينجح ويثبت wouter مع الباتش
2. npx tsc --noEmit نظيف
3. pnpm test — 91+ اختباراً (بما فيها agentPersona.test.ts الجديد)
4. webdev_save_checkpoint جديد → النشر ينجح
