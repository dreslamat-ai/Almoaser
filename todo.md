# المعاصر SaaS — قائمة المهام

## قاعدة البيانات والـ Schema
- [x] إضافة جدول subscriptions (الاشتراكات)
- [x] إضافة جدول plans (الباقات)
- [x] إضافة جدول tasks (المهام المحاسبية)
- [x] إضافة جدول service_invoices (فواتير الخدمة)
- [x] إضافة جدول registration_requests (طلبات التسجيل)
- [x] تشغيل pnpm db:push وتطبيق Schema على قاعدة البيانات
- [x] إدراج الباقات الثلاث (أساسية 299، احترافية 699، مؤسسية 1499)

## Landing Page (الصفحة الرئيسية)
- [x] تحديث الهوية البصرية بألوان المعاصر (Navy Blue + Gold)
- [x] بناء Hero Section بهوية المعاصر
- [x] قسم الخدمات الست
- [x] قسم الباقات الثلاث مع الأسعار والمزايا
- [x] قسم كيف نعمل (6 خطوات)
- [x] نموذج طلب الاستشارة المجانية
- [x] Footer بهوية المعاصر مع معلومات التواصل

## نظام المصادقة والتسجيل
- [x] تسجيل الدخول عبر Manus OAuth
- [x] حماية صفحات لوحة التحكم
- [x] ربط OAuth بنظام الاشتراكات

## لوحة تحكم العميل (Dashboard)
- [x] صفحة Overview (ملخص الحساب والاشتراك)
- [x] صفحة المهام (Tasks) — عرض وإنشاء وتحديث المهام المحاسبية
- [x] صفحة الفواتير (Invoices) — فواتير الخدمة الشهرية
- [x] صفحة الاشتراك (Subscription) — اختيار الباقة والترقية

## لوحة تحكم الإدارة (Admin)
- [x] عرض طلبات التسجيل مع بيانات التواصل
- [x] إدارة الاشتراكات مع أسماء الباقات
- [x] إدارة المهام الكلية مع تسميات عربية
- [x] إحصائيات عامة (4 بطاقات)

## الخلفية (Backend)
- [x] إجراءات tRPC للباقات (plans.list)
- [x] إجراءات tRPC للاشتراكات (subscription.create, get, upgrade)
- [x] إجراءات tRPC للمهام (tasks.create, list, updateStatus)
- [x] إجراءات tRPC للفواتير (invoices.list)
- [x] إجراءات tRPC للتسجيل (register.submit)
- [x] إجراءات tRPC للإدارة (admin.registrations, subscriptions, tasks)

## الاختبارات
- [x] اختبار auth.logout يمر بنجاح
- [x] اختبارات ERPNext connection (5/5 tests passed)

## ربط ERPNext (مكتمل)
- [x] حفظ بيانات الاتصال كـ Secrets (ERPNEXT_URL, ERPNEXT_USERNAME, ERPNEXT_PASSWORD)
- [x] بناء Backend proxy مع session caching (erpnext router في server/routers.ts)
- [x] إجراءات tRPC: testConnection, getCompanyInfo, getAccounts, getItems, getJournalEntries
- [x] صفحة ERPNextDashboard.tsx تعرض بيانات حقيقية من demo.almoaser.cloud
- [x] إضافة Route /erp في App.tsx
- [x] إضافة رابط "نظام ERP" في Sidebar لوحة التحكم

## Backlog (مرحلة لاحقة)

## داشبورد ERPNext الاحترافي + وكيل الذكاء الاصطناعي + PWA (مكتمل)
- [x] داشبورد احترافي مع KPIs (إيرادات، فواتير، عملاء، موردين/أصناف)
- [x] رسوم بيانية: Area Chart للإيرادات الشهرية، Pie Chart لحالة الفواتير، Bar Chart للمقارنة
- [x] جدول آخر الفواتير وجدول العملاء في الداشبورد
- [x] صفحة وكيل الذكاء الاصطناعي /agent مع اقتراحات جاهزة وتاريخ محادثة
- [x] صفحة إعدادات القنوات /channels مع ربط واتساب وتيليجرام واختبار مباشر
- [x] channels router في backend (saveSettings, testWhatsapp, testTelegram)
- [x] دعم PWA كامل: manifest.json + أيقونات + install banner في DashboardLayout
- [x] الداشبورد هو الصفحة الرئيسية (/)
- [x] تحديث DashboardLayout بعنوان "AI × ERPNext" وقائمة تنقل كاملة

- [ ] (مؤجل/blocked) ربط نظام دفع (Stripe/Moyasar) — لم يُنفذ؛ يتطلب مفاتيح خارجية وطلباً صريحاً من المستخدم

## إصلاح واجهة لوحة التحكم (بلاغ المستخدم 2026-07-17)
- [x] إصلاح عدم ظهور زر القائمة/الشريط الجانبي وزر تسجيل الخروج على الشاشات الصغيرة (الجوال)
- [x] التحقق بلقطات شاشة بمقاس جوال أن القائمة وتسجيل الخروج يظهران ويعملان

## أزرار الإجابات السريعة في محادثة الوكيل (طلب المستخدم 2026-07-17)
- [x] الخلفية: system prompt وبنية استجابة تجعل الوكيل يرفق خيارات إجابة مقترحة (quick_replies) مع كل سؤال تأكيد/استيضاح
- [x] حفظ خيارات الإجابة مع رسالة الوكيل في سجل المحادثات واستعادتها
- [x] الواجهة: عرض أزرار الخيارات تحت رسالة الوكيل وإرسال الخيار بضغطة واحدة دون كتابة
- [x] إخفاء الأزرار بعد الاختيار أو عند إرسال رسالة جديدة
- [x] اختبارات vitest لاستخراج quick_replies وحفظها
- [x] تأثير تحميل مرئي عند الضغط على زر الإجابة السريعة: الزر المختار يظهر مؤشر دوران وبقية الأزرار تُعطَّل فوراً

## إشعار صوتي عند اكتمال رد الوكيل (طلب المستخدم 2026-07-18)
- [x] نغمة قصيرة لطيفة تصدر فور اكتمال رد الوكيل وظهور الإجابة (Web Audio API دون ملفات خارجية)
- [x] زر كتم/تفعيل الصوت في رأس صفحة المحادثة مع حفظ التفضيل في localStorage

## تغيير رابط المشروع + إصلاح فشل النشر (طلب المستخدم 2026-07-18)
- [x] إصلاح فشل البناء في النشر: رفع workbox.maximumFileSizeToCacheInBytes في إعداد vite-plugin-pwa لأن index.js تجاوز 2MiB
- [x] التحقق من نجاح النشر (deployment) بعد إصلاح workbox وليس البناء المحلي فقط — الموقع يستجيب 200 وسجلات الإنتاج تظهر تشغيل الخادم الجديد 20:24
- [x] تغيير نطاق المشروع إلى بادئة almoaser — BLOCKED: لا يمكن تنفيذه من بيئة الوكيل؛ يتطلب إجراء المستخدم يدوياً من Management UI ← Settings ← Domains (تم إرشاده بالخطوات في الرسالة النهائية 2026-07-18)

## زر تثبيت التطبيق PWA (طلب المستخدم 2026-07-18)
- [x] مكوّن InstallAppButton: يلتقط beforeinstallprompt ويعرض prompt أصلي على كروم/أندرويد
- [x] تعليمات تثبيت لسفاري/iOS (مشاركة ← إضافة إلى الشاشة الرئيسية) عبر Dialog
- [x] إخفاء الزر تلقائياً إذا كان التطبيق مثبتاً (display-mode: standalone / appinstalled)
- [x] دمج الزر في الشريط الجانبي (سطح المكتب) ودرج القائمة (الجوال) في صفحات لوحة التحكم — الزر داخل SidebarContent المشتركة بين كل صفحات اللوحة (Dashboard/Tasks/Invoices/Subscription/AdminPanel/AccountSettings)

## ربط تسجيل الدخول بحسابات ERPNext (طلب المستخدم 2026-07-17)
- [x] إجراء tRPC لتسجيل الدخول بالتحقق من بيانات المستخدم مباشرة في ERPNext (frappe login)
- [x] ربط/إنشاء حساب المستخدم محلياً تلقائياً بنفس بريد ERPNext مع جلسة آمنة (كوكي موقّع JWT)
- [x] صفحة تسجيل دخول بالبريد وكلمة المرور بدلاً من دخول Manus OAuth
- [x] حماية الصفحات وإعادة التوجيه لغير المسجلين + تسجيل الخروج
- [x] اختبارات vitest لتدفق تسجيل الدخول بحساب ERPNext

## تسجيل مستخدم جديد مع باقة وتجربة 3 أيام (طلب المستخدم 2026-07-17)
- [x] إجراء تسجيل جديد: رابط نظام ERPNext + بريد + كلمة مرور، مع تحقق مباشر على نظام المستخدم
- [x] حفظ اتصال ERPNext الخاص بالمستخدم تلقائياً عند التسجيل (مشفراً)
- [x] اختيار الباقة عند التسجيل + إنشاء اشتراك بفترة تجريبية 3 أيام قبل بدء الباقة
- [x] صفحة إنشاء حساب جديد بخطوات (بيانات النظام ← اختيار الباقة)
- [x] عرض حالة التجربة والأيام المتبقية بعد الدخول (لوحة التحكم تعرض الاشتراك، والتفعيل التلقائي يحدث عند جلبه)
- [x] اختبارات vitest لتدفق التسجيل والتجربة
- [x] إعادة الصفحة الرئيسية "/" إلى صفحة Home التعريفية القديمة
- [x] التوجيه إلى لوحة التحكم بعد تسجيل الدخول/إنشاء الحساب
- [x] توحيد جميع شاشات عدم المصادقة على /login بدلاً من Manus OAuth في كل الصفحات المحمية
- [x] تفعيل الباقة المختارة تلقائياً بعد انتهاء التجربة (trial → active بدورة شهرية) مع اختبار يغطي الانتقال

## بلاغ: الوكيل يفرق بين الهمزات (ا/أ/إ/آ) فينشئ عملاء مكررين
- [x] إصلاح normalizeArabic ليوحد الهمزات (أ/إ/آ→ا) والتاء المربوطة (ة→ه) والألف المقصورة (ى→ي) وإزالة التشكيل قبل المطابقة
- [x] التأكد أن البحث في find_customer/find_item وfindSimilar* يستخدم التطبيع في الطرفين (buildSearchVariants + erpSearchByField يبحثان بكل أشكال الهمزات في ERPNext)
- [x] تحديث system prompt: التأكيد على أن اختلاف الهمزات لا يعني عميلاً مختلفاً
- [x] اختبارات vitest لحالات الهمزات (اسلام/إسلام/أسلام) — 28 اختباراً ناجحاً
- [x] معالجة العميل المكرر "إسلام": إلغاء ACC-SINV-2026-00005، إنشاء واعتماد ACC-SINV-2026-00006 للعميل الأصلي "اسلام" (35,000)، وتعطيل العميل المكرر (disabled=1) لأن ERPNext يمنع حذفه لارتباطه بمستندات
- [x] نقل قسم اتصال ERPNext (الرابط + اليوزر + الباسوورد) من /settings إلى صفحة إعدادات الربط /channels ليكون مع إعدادات القنوات

## نقل إعدادات اتصال ERPNext إلى صفحة القنوات (موافقة المستخدم 2026-07-17)
- [x] إضافة قسم "اتصال ERPNext" في أعلى صفحة /channels (رابط + مستخدم + كلمة مرور + اختبار + حذف + شارة متصل)
- [x] إزالة قسم اتصال ERPNext من صفحة /settings مع إبقاء رابط توجيهي لصفحة القنوات

## حفظ سجل محادثات الوكيل (طلب المستخدم 2026-07-17)
- [x] جدولا agent_conversations وagent_messages في قاعدة البيانات مع الترحيل
- [x] إجراءات tRPC: قائمة المحادثات، رسائل محادثة، إنشاء/حذف محادثة، وحفظ الرسائل تلقائياً أثناء agent.chat
- [x] واجهة AgentChat: شريط جانبي بالمحادثات السابقة + محادثة جديدة + استكمال محادثة + حذف
- [x] اختبارات vitest لسجل المحادثات
- [x] إشعارات للمسؤول عند تسجيل عميل جديد — نُفذت عبر نظام إشعارات المالك المدمج في Manus (notifyOwner) بدلاً من البريد الإلكتروني
- [x] صفحة إعدادات حساب العميل (/settings — AccountSettings.tsx + profile router)
- [x] تصدير الفواتير كـ PDF (تحميل مباشر من ERPNext)
- [x] تعليقات المحاسب على المهام (tasks.comments/addComment + TaskComments في Tasks.tsx)
- [x] إضافة أدوار Customer/Sales Invoice للمستخدم ai في ERPNext (تم من قِبل المستخدم)
- [x] عرض فواتير المبيعات والعملاء عند توفر الصلاحيات

## وكيل الذكاء الاصطناعي الحقيقي (Function Calling)
- [x] بناء server/routers/agent.ts بـ Function Calling حقيقي (6 أدوات)
- [x] أداة get_invoices: جلب الفواتير مع فلترة
- [x] أداة get_invoice_detail: تفاصيل فاتورة محددة مع الأصناف
- [x] أداة get_customers: قائمة العملاء
- [x] أداة get_items: قائمة الأصناف والخدمات
- [x] أداة create_invoice: إنشاء فاتورة مبيعات
- [x] أداة get_sales_report: تقرير المبيعات
- [x] إعادة بناء AgentChat.tsx لعرض نتائج الأدوات (جداول تفاعلية + PDF)
- [x] إضافة أداة create_customer للوكيل (إنشاء عميل جديد)
- [x] إضافة أداة create_item للوكيل (إنشاء صنف جديد)
- [x] إضافة أداة submit_invoice للوكيل (اعتماد الفواتير)
- [x] تحديث System Prompt: الوكيل يعالج النواقص تلقائياً (عميل/صنف غير موجود → ينشئه)
- [x] إعادة تسمية النظام إلى Almoaser AI ERP في كل الواجهات
- [x] إضافة شعار المعاصر (almoaser.com) في النظام

## مراجعة الهوية البصرية النهائية
- [x] استبدال جميع النصوص المتبقية (Almoaser AI Powered ERP / ERPNext) بـ Almoaser AI ERP في الصفحات والـ meta tags
- [x] توحيد شعار المعاصر في الصفحات القديمة (Dashboard/Tasks/Invoices/Subscription/Admin)

## إصلاح أخطاء الوكيل
- [x] إصلاح خطأ "Cannot read properties of undefined (reading '0')" عند إنشاء فاتورة عبر الوكيل

## ترقية الوكيل إلى وكيل خبير
- [x] حلقة أدوات متعددة الخطوات (multi-step tool loop) بدل جولة واحدة (8 جولات)
- [x] تحقق تلقائي من وجود العميل والصنف قبل إنشاء الفاتورة وإنشاؤهما عند الحاجة
- [x] System Prompt خبير محاسبي شامل (سياق، قواعد عمل، أمثلة، لغتان عربي/إنجليزي)
- [x] معالجة أخطاء ذكية: ترجمة أخطاء ERPNext لرسائل عربية مفهومة مع اقتراح الحل
- [x] اختبار سيناريو "محمود / عدد 1 خدمة استشارية بسعر 65000" بنجاح (ACC-SINV-2026-00001 بقيمة 65000)
- [x] منع تكرار العملاء/الأصناف: بحث تقريبي أولاً، استخدام الموجود، اقتراح المشابه، وإنشاء جديد فقط عند عدم وجود تطابق
- [x] إصلاح normalizeMessage في llm.ts لتمرير tool_calls في رسائل assistant (تفعيل حلقة الأدوات متعددة الخطوات فعلياً)
- [x] اختبار E2E عبر agent.chat الفعلي: سيناريو محمود/خدمة استشارية (get_customers → get_items → create_invoice) نجح
- [x] اختبار E2E: صنف غير موجود → إنشاء تلقائي (create_item) ثم إكمال الفاتورة — نجح
- [x] اختبار E2E: منع تكرار عميل موجود (duplicate_prevented) — نجح
- [x] قاعدة لغة ثنائية صريحة في system prompt (عربي/إنجليزي حسب لغة المستخدم)

## توسيع صلاحيات الوكيل الكاملة (طلب المستخدم 2026-07-17)
- [x] أداة create_payment_entry: تسجيل دفعة مستلمة (من عميل) أو مدفوعة (لمورد) مع الاعتماد
- [x] أداة create_journal_entry: تسجيل قيد يومية (مدين/دائن) مع الاعتماد
- [x] أداة create_purchase_invoice: إنشاء فاتورة مشتريات من مورد
- [x] أداة create_supplier + get_suppliers: إدارة الموردين مع منع التكرار
- [x] أداة submit_document عامة: اعتماد أي مستند (فاتورة مبيعات/مشتريات/قيد/دفعة)
- [x] أدوات استعلام: get_payments وget_journal_entries وget_purchase_invoices وget_accounts
- [x] تحديث system prompt ليغطي كل العمليات الجديدة

## إعدادات اتصال ERPNext لكل عميل
- [x] جدول erpnext_connections في قاعدة البيانات (رابط + مستخدم + كلمة مرور مشفرة AES-256-GCM لكل مستخدم)
- [x] واجهة في صفحة الإعدادات لإدخال واختبار بيانات اتصال ERPNext الخاصة بالعميل
- [x] الوكيل وكل استدعاءات ERPNext تستخدم اتصال العميل الحالي (fallback لاتصال النظام الافتراضي للمالك)

## صلاحيات التعديل والحذف الكاملة للوكيل (طلب المستخدم 2026-07-17)
- [x] أداة update_document موحدة: تعديل فواتير (مسودة)، عملاء، موردين، أصناف، قيود، دفعات — مع منع تعديل المعتمد
- [x] أداة cancel_document: إلغاء المستندات المعتمدة (docstatus 1 → 2)
- [x] أداة delete_document: حذف أي مستند (مع إلغاء تلقائي للمعتمد قبل الحذف) — اختبار E2E: إنشاء/تعديل/حذف صنف نجح
- [x] تحديث system prompt: قواعد التعديل/الحذف + طلب تأكيد المستخدم قبل الحذف أو الإلغاء
- [x] بطاقات عرض نتائج التعديل/الإلغاء/الحذف في واجهة المحادثة

## إدارة مستخدمي النظام
- [x] تبويب "المستخدمون" في لوحة الإدارة: عرض جميع المستخدمين + ترقية/تخفيض الدور (مسؤول/عميل) مع حماية من إزالة صلاحية المسؤول الذاتية
- [x] تعطيل/إعادة تفعيل المستخدمين: حقل isActive في قاعدة البيانات + procedure + زر في الواجهة + حظر المعطلين من الدخول عبر middleware
- [x] اختبارات vitest لإدارة المستخدمين (admin.users / setUserRole / setUserActive)

## بلاغات المستخدم (2026-07-17)
- [x] إصلاح: رابط الفواتير ورابط التقارير في الواجهة لا يعملان
- [x] صفحة /erp/invoices: قائمة فواتير المبيعات من ERPNext مع الحالة والفلترة
- [x] صفحة /erp/customers: قائمة العملاء من ERPNext
- [x] صفحة /erp/reports: تقارير المبيعات (إجمالي/مدفوع/غير مدفوع + رسوم بيانية)

## الإدخال الصوتي للوكيل (طلب المستخدم 2026-07-17)
- [x] زر ميكروفون في صفحة الوكيل: تسجيل صوتي → تحويل لنص (Whisper) → إرسال للوكيل وتنفيذ
- [x] رفع صورة فاتورة/سند قبض في محادثة الوكيل → قراءتها بالذكاء الاصطناعي (Vision OCR) → استخراج البيانات → تأكيد المستخدم → تسجيلها في النظام

## نظام الإشعارات المخصصة — جرس + Web Push (طلب المستخدم 2026-07-18)
- [x] جدول notifications في قاعدة البيانات (userId, type, title, body, link, readAt, createdAt)
- [x] جدول push_subscriptions لاشتراكات Web Push (endpoint, keys, userId)
- [x] دوال db: إنشاء إشعار، قائمة إشعارات المستخدم، عدّاد غير المقروء، وسم مقروء/الكل، حذف
- [x] إجراءات tRPC: notifications.list / unreadCount / markRead / markAllRead / delete
- [x] إطلاق إشعار عند إنشاء/اعتماد فاتورة عبر الوكيل
- [x] إطلاق إشعار عند اكتمال مهمة
- [x] إشعار اقتراب انتهاء الفترة التجريبية (عند جلب الاشتراك إذا تبقى يوم أو أقل)
- [x] إشعار للأدمن عند تسجيل مستخدم جديد
- [x] Web Push: مفاتيح VAPID كأسرار + مكتبة web-push في الخادم
- [x] إجراءات tRPC للاشتراك/إلغاء اشتراك push وحفظه في قاعدة البيانات
- [x] إرسال push عند إنشاء أي إشعار (لكل اشتراكات المستخدم مع تنظيف المنتهية)
- [x] service worker: معالجة حدث push وعرض الإشعار وفتح الرابط عند النقر
- [x] واجهة: جرس إشعارات مع عدّاد غير المقروء في لوحة التحكم (سطح المكتب + جوال)
- [x] واجهة: قائمة إشعارات مع وسم مقروء/الكل ووقت نسبي وروابط
- [x] واجهة: زر تفعيل إشعارات الجهاز (طلب الإذن + الاشتراك)
- [x] اختبارات vitest لدوال الإشعارات والإجراءات

## GitHub
- [x] رفع كود المشروع إلى مستودع GitHub خاص (dreslamat-ai/almoaser-ai-agent) عبر PAT من المستخدم
