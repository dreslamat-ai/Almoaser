# ملاحظات عمل — إصلاح وترقية وكيل ERPNext (2026-07-17)

## ما تم إنجازه
- إصلاح خطأ agent.ts:455 "Cannot read properties of undefined (reading '0')" — كان `response.choices[0]` بدون حماية عند فشل/فراغ استجابة LLM. أضيفت حماية `response?.choices?.[0]?.message` مع try/catch حول invokeLLM ورسائل خطأ عربية.
- إضافة normalizeArabic + isSimilar + findSimilarCustomers + findSimilarItems في server/routers/agent.ts
- create_invoice الآن يحل العميل والصنف تلقائياً (بحث تقريبي)، يعيد needs_clarification مع candidates عند تعدد المرشحين، ويعيد error واضح عند عدم الوجود
- create_customer/create_item يرفضان الإنشاء عند وجود مشابه (duplicate_prevented + candidates)
- get_items يدعم search الآن، get_customers كان يدعمه
- translateErpError لترجمة أخطاء ERPNext (LinkValidation/Duplicate/Mandatory/Permission/Validation)
- الحلقة رُفعت إلى 8 جولات
- System prompt محدث بقواعد منع التكرار وسير العمل الصحيح
- اختبارات vitest في server/agent.matching.test.ts — 15 اختبار ناجح
- E2E مباشر عبر ERP API: أُنشئ صنف "خدمة استشارية" وفاتورة ACC-SINV-2026-00001 لمحمود بقيمة 65000 (Draft) — بيانات فعلية باقية في ERPNext
- ERPNext يحتوي: عملاء (محمود، عميل اختبار الوكيل)، أصناف (خدمة اختبار الوكيل بسعر 50، خدمة استشارية بسعر 65000)

## فجوات يجب حلها قبل checkpoint (من مراجعة النظام)
جميع الفجوات حُلّت: (1) normalizeMessage يمرر tool_calls الآن، (2) اختبار E2E عبر agent.chat نجح بثلاثة سيناريوهات (فاتورة بصنف موجود ACC-SINV-2026-00003، إنشاء صنف تلقائي ثم فاتورة ACC-SINV-2026-00004، منع تكرار عميل محمود)، (3) قاعدة لغة ثنائية صريحة أضيفت. الفواتير والأصناف التجريبية حُذفت من ERPNext (بقي عميل محمود وصنف خدمة استشارية كبيانات فعلية للمستخدم).

## طلبات المستخدم الجديدة (2026-07-17 المرحلة الثانية)
1. توسيع صلاحيات الوكيل: دفعات (Payment Entry)، قيود يومية (Journal Entry)، فواتير مشتريات + دفعاتها، موردين، اعتماد كل المستندات
2. إعدادات اتصال ERPNext لكل عميل (رابط + يوزر + باسوورد) تُسجل من لوحة التحكم/الإعدادات
3. إدارة/تسجيل مستخدمين للنظام
4. إصلاح روابط الفواتير والتقارير (تم: أنشأت ErpInvoices/ErpCustomers/ErpReports وسجلت /erp/invoices|customers|reports في App.tsx)
5. إدخال صوتي للوكيل (Whisper) — لم يبدأ. اقرأ /home/ubuntu/skills/webdev-voice-transcription/SKILL.md
6. رفع صورة فاتورة/سند قبض → Vision OCR → استخراج → تأكيد → تسجيل — لم يبدأ. اقرأ /home/ubuntu/skills/webdev-llm-integration/SKILL.md (vision) و webdev-file-storage

## حالة التقدم (2026-07-17 03:10)
- [تم] كل أدوات الوكيل الجديدة (موردين/مشتريات/دفعات/قيود/اعتماد) + system prompt محدث + tsc نظيف
- [تم] صفحات /erp/invoices و/erp/customers و/erp/reports — تعمل (لقطات الشاشة أظهرت شاشة تسجيل الدخول فقط لأن الجلسة غير موثقة في أداة اللقطات، الصفحات مسجلة في الراوتر بشكل صحيح)
- ملاحظة: اللقطات ظهرت كصفحة sign-in لأن DashboardLayout يتطلب مصادقة — هذا سلوك طبيعي
- [متبقي] الصوت، صورة الفاتورة OCR، إعدادات اتصال ERPNext لكل عميل (جدول erpnext_connections)، إدارة المستخدمين، checkpoint
- صفحة الوكيل: client/src/pages/AgentChat.tsx | erpnext router في server/routers.ts (سطر ~267) | agent router في server/routers/agent.ts

## معلومات تنفيذ الصوت والصور (2026-07-17 03:15)
- agent.ts: agentRouter عند سطر 968 — chat (protectedProcedure) + getInvoicePdf (سطر 1127). حلقة الأدوات تعمل حتى 8 جولات، SYSTEM prompt عند 983-1046
- AgentChat.tsx: الإدخال عند 523-548 (Textarea + زر Send)، دالة send() عند 385-409، Message type عند 16-22
- voiceTranscription helper جاهز: server/_core/voiceTranscription.ts → transcribeAudio({ audioUrl, language, prompt }) — يقبل URL؛ حد 16MB؛ صيغ webm/mp3/wav/ogg/m4a
- storagePut(relKey, buffer, contentType) من server/storage.ts → { key, url } حيث url = /manus-storage/{key}
- invokeLLM يدعم vision: content: [{type:"text"},{type:"image_url", image_url:{url}}] — server/_core/llm.ts يمرر multi-part content
- خطة: أضيف إلى agentRouter: transcribeVoice (base64 audio → storagePut → transcribeAudio → text) + extractDocument (base64 image → storagePut → invokeLLM vision مع json_schema → بيانات مستخرجة) والواجهة: زر ميكروفون (MediaRecorder webm) + زر صورة (file input) في AgentChat
- الاستخراج يرجع: doc_type (sales_invoice|purchase_invoice|receipt_voucher), party, date, items[], amounts، ثم يُحقن كنص في محادثة الوكيل ليؤكد المستخدم ثم ينفذ الوكيل بأدواته المعتادة

## نتائج التشخيص (2026-07-17 03:30)
- [مُنفذ] transcribeVoice + extractDocument في agent.ts (بعد getInvoicePdf) — tsc نظيف
- [مُنفذ] AgentChat.tsx: زر Mic (MediaRecorder webm) + زر ImagePlus (file input) + ملخص استخراج يطلب تأكيد "نعم"
- [مُنفذ] system prompt قاعدة 11: تأكيد المستند المستخرج → create_invoice/create_purchase_invoice/create_payment_entry
- **باگ مُصلح**: storagePut يضيف لاحقة عشوائية للمفتاح (appendHashSuffix) — يجب استخدام key المُعاد وليس fileKey الأصلي عند storageGetSignedUrl، وإلا 403 والنموذج يرى "NO IMAGE AVAILABLE"
- اختبار: /tmp/test-invoice.png (فاتورة تجريبية 78750 OMR لـ Mahmoud Trading Co) + test-ocr-e2e.mjs (يستدعي extractDocument ثم chat مع "نعم سجّلها" عبر superjson/jose cookie app_session_id) + debug-ocr.mjs
- تشغيل الاختبار: node -r dotenv/config test-ocr-e2e.mjs (لا تستخدم source .env — ممنوع)
- gap reminder: يطلب إثبات سيناريو تأكيد→تسجيل E2E قبل checkpoint — الاختبار جارٍ
- بنود متبقية بعد ذلك: erpnext_connections لكل عميل (سطور todo 125-127)، إدارة المستخدمين (130)، بنود قديمة (71-75)

## حالة تنفيذ erpnext_connections (2026-07-17 03:25)
- [تم] checkpoint 51b6e3f6 منشور: صوت + OCR + صفحات /erp/* + due_date fix — أُبلغ المستخدم
- [تم] drizzle/schema.ts: جدول erpnextConnections (id,userId,url,username,passwordEnc,lastVerifiedAt,createdAt,updatedAt) + db:push نجح والجدول موجود
- [تم] server/erpConnection.ts: encryptPassword/decryptPassword (AES-256-GCM بمفتاح sha256 من JWT_SECRET) + getErpConfigForUser(userId) يرجع اتصال المستخدم أو env fallback + getErpSession(config) كاش لكل url|username + invalidateErpSession + testErpConnection(url,user,pass)
- [متبقي 1] ربط agent.ts وroutes.ts باتصال المستخدم: حالياً getSession/erpGET/erpPOST/erpPUT في agent.ts (سطور 14-62) وgetErpnextSession/erpFetch في routers.ts (سطور 30-80) تستخدم env مباشرة — يجب تمرير ErpConfig عبر ctx.user.id. خطة: تحويل erpGET/erpPOST/erpPUT لتقبل config كمعامل أول، وفي executeTool وchat mutation جلب config مرة عبر getErpConfigForUser(ctx.user.id)
- [متبقي 2] tRPC procedures: erpConnection.get/save/test/delete في routers.ts (protectedProcedure) — لا تُرجع كلمة المرور أبداً (فقط hasPassword: true)
- [متبقي 3] واجهة AccountSettings.tsx (client/src/pages/): قسم "اتصال ERPNext" — حقول url/username/password + زر اختبار الاتصال + حفظ
- [متبقي 4] إدارة المستخدمين: admin procedures (listUsers, updateRole) + صفحة AdminUsers.tsx + مسار /admin/users
- [متبقي 5] بنود todo قديمة 71-75: Stripe (مؤجل—تكامل خارجي)، إشعار بريد للمسؤول عند تسجيل عميل (استخدم notifyOwner من server/_core/notification.ts)، تعليقات المحاسب (تم تنفيذها سابقاً — تحقق)، صفحة إعدادات حساب العميل (موجودة AccountSettings.tsx — تحقق ثم علّم)
- ملاحظة: getDb مستورد من ./db في erpConnection.ts — تحقق أن db.ts يصدّر getDb

## بنية agent.ts الحالية (662 سطر)
- سطر 11-33: getSession() — كاش sid عالمي، يقرأ ERPNEXT_URL/USERNAME/PASSWORD من env
- سطر 35-63: erpGET/erpPOST (تعيد المحاولة عند 401/403)
- سطر 66-228: TOOLS: get_invoices, get_invoice_detail, get_customers, get_items, create_invoice, get_sales_report, create_customer, create_item, submit_invoice
- بعدها: normalizeArabic, isSimilar, findSimilarCustomers, findSimilarItems, translateErpError, executeTool (switch), ثم agent router: chat (حلقة 8 جولات), getInvoicePdf
- submit_invoice ينفذ عبر PUT docstatus:1 (تحقق من التنفيذ الفعلي في executeTool)
- خطة إعدادات الاتصال لكل عميل: جدول erpnext_connections (userId, url, username, password مشفرة AES via JWT_SECRET, isActive)، getSession يقبل بيانات اتصال per-user مع كاش map حسب userId، fallback إلى env للمالك
- إدارة المستخدمين: يوجد role admin/user في جدول users، يحتاج صفحة admin لعرض المستخدمين وتغيير الدور
- صفحة الإعدادات موجودة: client/src/pages/AccountSettings.tsx على /settings (فيها profile router)

## معلومات تقنية
- invokeLLM في server/_core/llm.ts سطر ~340، normalizeMessage سطر ~141 (يعيد role/name/content فقط لرسائل غير tool)
- dev server: https://3000-i0cjapuj73b92k88nwbf0-8e732426.sg1.manus.computer
- production: erpnextai-wypcefat.manus.space (auto-publish مفعّل)
- آخر checkpoint: 4924cb23
- بقية العمل السابق مكتمل: تعليقات المهام (tasks.comments/addComment + TaskComments في Tasks.tsx)، صفحة /settings (AccountSettings.tsx + profile router)، إشعار المالك عند التسجيل والتعليقات (notifyOwner)
## حالة per-user ERP connection في agent.ts (2026-07-17 03:30)
- [تم] agent.ts: AsyncLocalStorage erpContext + runWithErpConfig(userId, fn) + currentErpConfig() + getSession يستخدم getErpSession(config) + erpBaseUrl() في كل المواضع (45,60,489,717,1145) — لا يتبقى process.env.ERPNEXT_URL إلا في fallback سطر 29
- [جارٍ] لفّ chat mutation بـ runWithErpConfig(ctx.user.id, ...) — يتطلب تغيير `.mutation(async ({ input }) =>` إلى `({ input, ctx })` سطر ~995 وإغلاق `});` قبل نهاية الحلقة (بعد return النهائي للحلقة، قبل `}),`)
- [متبقي] getInvoicePdf (سطر ~1145) يحتاج لف بـ runWithErpConfig أيضاً — mutation لديه ctx? تحقق
- [متبقي] routers.ts: getErpnextSession/erpFetch (سطور 30-80) لا تزال env-only — حدّثها لاستخدام getErpConfigForUser(ctx.user.id) أو اتركها للداشبورد مع ملاحظة
- [متبقي] tRPC procedures: erpConnection.get/save/test/delete + واجهة AccountSettings + أدوات update/cancel/delete للوكيل + admin users page
## حالة 2026-07-17 03:35 (بعد per-user connection)
- [تم] agent.ts: كل helpers تستخدم AsyncLocalStorage erpContext + runWithErpConfig؛ chat وgetInvoicePdf ملفوفان — TS نظيف
- [تم] routers.ts: erpFetch(path, userId) per-user، كل erpnext procedures أصبحت protectedProcedure مع ctx.user.id؛ createSalesInvoice وagentChat يستخدمان getErpConfigForUser
- [تم] erpConnection router: get/save (يختبر قبل الحفظ)/test/remove — كلمة المرور مشفرة ولا تُعاد للواجهة
- [تم] AccountSettings.tsx: قسم اتصال ERPNext كامل (حفظ/اختبار/حذف + إظهار كلمة المرور)
- [متبقي] transcribeVoice/extractDocument لا يحتاجان لف (لا يلمسان ERP مباشرة)
- [متبقي] أدوات update/cancel/delete للوكيل (المرحلة 2) + admin users page (المرحلة 3) + اختبارات + checkpoint

## حالة 2026-07-17 03:40 (بعد أدوات التعديل/الحذف)
- [تم] agent.ts: erpPUT/erpDELETE/cancelDoc helpers + أدوات update_document/cancel_document/delete_document في TOOLS وexecuteTool
- [تم] update_document يمنع تعديل معتمد (docstatus=1) برسالة توجيهية؛ delete_document يلغي المعتمد تلقائياً قبل الحذف
- [تم] system prompt: قواعد 12 (تعديل) و13 (حذف مع تأكيد إلزامي) + قسم صلاحيات موسع
- [تم] AgentChat.tsx: بطاقات __DOC_UPDATED__/__DOC_CANCELLED__/__DOC_DELETED__
- [متبقي] المرحلة 3: إدارة مستخدمي النظام (admin page لعرض المستخدمين وتغيير الأدوار) — users table موجود مع role enum
- [متبقي] اختبار E2E للأدوات الجديدة + vitest + checkpoint
- بنية agent chat: display markers تُعرض عبر ToolResultRenderer في AgentChat.tsx (السطر ~308)

## بلاغ الهمزات (2026-07-17 04:00) — التشخيص والحل
- normalizeArabic (سطر 523) صحيح محلياً (يوحد أ/إ/آ→ا، ى→ي، ة→ه)
- **السبب الجذري**: findSimilarCustomers/Items/Suppliers تبحث في ERPNext بفلتر `like %firstWord%` بالنص الخام. "اسلام" لا يطابق "إسلام" في like بقاعدة البيانات → صفر نتائج → الفلترة المحلية لا تعمل → الوكيل ينشئ عميلاً مكرراً
- **الحل المنفذ**: buildSearchVariants(word) يولد متغيرات الهمزات (ا/أ/إ/آ في أول حرف + ه/ة وي/ى في آخر حرف) ويبحث بـ or_filters أو استعلامات متوازية ثم يدمج ويفلتر بـ isSimilar
- معالجة العميل المكرر: العميل "إسلام" أُنشئ بالخطأ — تحقق من مستنداته ثم عطّله/احذفه

## دمج العميل المكرر (2026-07-17 04:10)
- الوضع قبل: "اسلام" (أصلي، بلا فواتير) + "إسلام" (مكرر، عليه ACC-SINV-2026-00005 معتمدة 35000)
- المستخدم وافق: إلغاء الفاتورة، إعادة إنشائها لـ"اسلام"، حذف "إسلام"
- نتيجة التنفيذ في /tmp/merge-result.txt
- إصلاح الهمزات: buildSearchVariants + erpSearchByField في agent.ts (سطور ~543-590) — 28 اختباراً ناجحاً
