// ─── بيئة الاختبارات ──────────────────────────────────────────────────────────
//
// vitest لا يقرأ .env من تلقاء نفسه، فكانت عشرة اختبارات تفشل بلا سبب ظاهر:
// getDb() ترجع null بلا DATABASE_URL، والتأكيد يصير "expected false to be true"
// دون ذكر القاعدة أصلاً. ظهر ذلك كأنه ارتداد في المنطق وهو غياب متغيّر بيئة —
// وهذا بالضبط النوع من الضجيج الذي يجعل فريقاً يتعوّد على تجاهل الاختبارات.
//
// dotenv لا bash: السطر EMAIL_FROM يحوي أقواساً زاوية تكسر `source .env`.
import { config } from "dotenv";
config();
